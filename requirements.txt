## Runtime
# Document parsing (requires pydantic v2 under the hood)
docling
pydantic>=2.6,<3

# Progress bar
tqdm==4.66.4

# Excel tools used by auxiliary scripts (keep installed)
openpyxl>=3.1.5,<4

## Dev/Test
pytest==8.2.2

## Notes
# - Python 3.9+ supported here. If you see urllib3/OpenSSL warnings on macOS (LibreSSL), they are cosmetic.
# - For GPU acceleration where supported, install a CUDA-enabled PyTorch in the same venv (optional).
