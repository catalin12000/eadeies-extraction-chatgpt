# Core dependencies
pydantic==1.10.15
python-dotenv==1.0.1
openai==0.28.1
pdfplumber==0.11.4

# Excel export
openpyxl==3.1.2

# Optional: docling (requires Python >=3.10). Not installed automatically due to current Python 3.9 env.
# To use docling benchmarking, recreate venv with Python 3.11+ and install: pip install docling

# Utilities
tqdm==4.66.4
